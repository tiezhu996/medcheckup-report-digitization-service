<!-- 改编自：医疗健康主题项目提示词/lp-341/lp-341.md；后端由 FastAPI (Python 3.11) 改造为 Go 1.22 + Gin + GORM -->
# lp-341 体检报告数字化平台

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**体检报告数字化平台（GbCheckup）** 为体检中心提供体检套餐配置、体检结果录入、报告自动生成与解读、异常指标追踪和团检管理的全流程数字化服务。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户）**
  - 字段：id, phone, password_hash, name, avatar, role（Admin/Doctor/FrontDesk/Examinee）, department, created_at
  - 关键关系：医生录入多个 ExamResult，前台创建多个 Registration，管理员维护 Package
  - 业务动作：注册/登录、修改资料、上传头像
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/repository/user_repository.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/userStore.ts → frontend/src/pages/Profile.tsx**

- **Package（体检套餐）**
  - 字段：id, name, package_type（入职体检/年度体检/高端体检等）, price, status, description, created_at
  - 关键关系：包含多个 PackageItem，被多个 Registration 引用
  - 业务动作：配置套餐、维护项目分组、设置价格
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **PackageItem（检查项目）**
  - 字段：id, package_id, item_name, item_group, ref_value_range, department, sort_order
  - 关键关系：隶属于 Package
  - 业务动作：配置检查项目、设置参考值范围与检查科室
  - **此实体必须贯穿全栈**

- **Examinee（体检人）**
  - 字段：id, name, id_card_no, phone, gender, age, source_type（个人/团体）, enterprise_id, created_at
  - 关键关系：拥有多个 Registration、ExamResult、Report、AbnormalMetric
  - 业务动作：登记体检人、团体批量导入、查询历次报告
  - **此实体必须贯穿全栈**

- **Registration（体检登记/导检）**
  - 字段：id, examinee_id, package_id, guide_no, status（Registered/InProgress/Completed）, registered_at, register_user_id
  - 关键关系：隶属于 Examinee，引用 Package
  - 业务动作：登记分配套餐、生成导检单号、每日人数统计与科室分流
  - **此实体必须贯穿全栈**

- **ExamResult（检查结果）**
  - 字段：id, registration_id, examinee_id, package_item_id, result_value, result_text, is_abnormal, status（Pending/Entered/Reviewed）, image_url, doctor_id, entered_at
  - 关键关系：隶属于 Registration 与 PackageItem
  - 业务动作：录入数值/文本/影像结果、与参考值比对标红异常、汇总至总检工作台
  - **此实体必须贯穿全栈**

- **Report（体检报告）**
  - 字段：id, registration_id, examinee_id, report_no, status（Draft/Generated/Reviewed/Published）, conclusion, health_advice, follow_up_reminder, pdf_url, doctor_id, generated_at
  - 关键关系：汇总 Registration 下所有 ExamResult
  - 业务动作：审核结果、生成 PDF 报告、添加健康指导、在线查看与下载
  - **此实体必须贯穿全栈**

- **AbnormalMetric（异常指标）**
  - 字段：id, examinee_id, package_item_id, abnormal_level（Mild/Moderate/Severe）, value, ref_value_range, trend_json, follow_up_status, specialist_advice
  - 关键关系：隶属于 Examinee 与 PackageItem
  - 业务动作：记录历次异常、生成趋势对比、设置复查提醒、关联专科建议
  - **此实体必须贯穿全栈**

- **Enterprise（团检企业）** 与 **GroupOrder（团检订单）**
  - Enterprise 字段：id, name, contact, phone, address
  - GroupOrder 字段：id, enterprise_id, package_id, examinee_count, status, report_delivery_status, created_at
  - 业务动作：企业档案管理、团检订单、报告批量交付
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/dashboard（运营统计）**
  - 消费实体：Package、Registration、Report、AbnormalMetric
  - 关键交互：各套餐成交量、科室工作量、异常检出率 TOP 疾病、收入统计报表
  - 共享组件：`<StatusBadge>`、`<AbnormalTag>`

- **/packages（体检套餐管理）**
  - 消费实体：Package、PackageItem
  - 关键交互：套餐列表、套餐详情（项目分组与参考值范围）、价格配置
  - 共享组件：`<StatusBadge>`、`<EmptyState>`

- **/registrations（体检登记与导检）**
  - 消费实体：Registration、Examinee、Package
  - 关键交互：登记体检人、分配套餐、团体批量导入、每日人数统计与科室分流
  - 共享组件：`<StatusBadge>`（与 /dashboard 共用）、`<EmptyState>`

- **/results（检查结果录入）**
  - 消费实体：ExamResult、PackageItem、Registration
  - 关键交互：数值型结果自动与参考值比对标红、文本型描述录入、影像图片上传、总检工作台汇总
  - 共享组件：`<AbnormalTag>`、`<ImageUploader>`

- **/reports（报告管理）**
  - 消费实体：Report、ExamResult
  - 关键交互：审核结果、生成 PDF、添加个性化健康指导、在线查看与下载
  - 共享组件：`<ReportStatusBadge>`、`<EmptyState>`

- **/abnormal-metrics（异常指标追踪）**
  - 消费实体：AbnormalMetric、Examinee
  - 关键交互：历次异常指标趋势对比、复查提醒、专科就诊建议
  - 共享组件：`<AbnormalTag>`（与 /results 共用）、`<EmptyState>`

- **/group-orders（团检管理）**
  - 消费实体：Enterprise、GroupOrder、Package
  - 关键交互：企业档案、团检订单、报告批量交付
  - 共享组件：`<StatusBadge>`、`<EmptyState>`

### 4. 横切关注点（≥ 2 个）

- **认证授权（JWT + RBAC）**
  - 触达文件层：
    - 数据库：`users` 表含 role 字段
    - 后端：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中报告审核、套餐配置等接口需 `AuthRequired()` 和 `RequireRole()`（前台只能登记，医生只能录入/审核）
    - 前端：`router/guards.tsx`、`components/common/RoleGuard.tsx`、`stores/authStore.ts`

- **PDF 报告生成与文件上传**
  - 触达文件层：
    - 后端：`internal/service/report_service.go`、`internal/util/pdf_generator.go`、`internal/middleware/upload.go`
    - 路由：报告生成、影像图片上传接口
    - 前端：`utils/request.ts`（文件下载）、`components/common/ImageUploader.tsx`

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - 前端：`utils/request.ts` 拦截器、`components/common/ErrorBoundary.tsx`

### 5. 共享枚举/常量（≥ 2 组）

- **AbnormalLevel（异常等级）**
  - 值：MILD = 'mild', MODERATE = 'moderate', SEVERE = 'severe'
  - 前端定义位置：`frontend/src/constants/report.ts`
  - 后端定义位置：`backend/internal/constants/report.go`
  - 出现位置：AbnormalMetric 模型、ExamResultService 比对逻辑、AbnormalTag、结果录入页、报告汇总、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **ReportStatus（报告状态）**
  - 值：DRAFT = 'draft', GENERATED = 'generated', REVIEWED = 'reviewed', PUBLISHED = 'published'
  - 前端定义位置：`frontend/src/constants/report.ts`
  - 后端定义位置：`backend/internal/constants/report.go`
  - 出现位置：Report 模型、ReportService 状态机、ReportStatusBadge、报告列表/详情、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **UserRole（用户角色）**
  - 值：ADMIN = 'admin', DOCTOR = 'doctor', FRONT_DESK = 'front_desk', EXAMINEE = 'examinee'
  - 前端定义位置：`frontend/src/constants/user.ts`
  - 后端定义位置：`backend/internal/constants/user.go`
  - 出现位置：User 模型、RBAC 中间件、路由权限、前端按钮显隐、路由守卫
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<StatusBadge>`：被 /dashboard、/packages、/registrations、/group-orders 共用
- `<AbnormalTag>`：被 /dashboard、/results、/abnormal-metrics 共用（按异常等级着色）
- `<ReportStatusBadge>`：被 /reports 使用
- `<ImageUploader>`：被 /results 使用（影像检查图片上传）
- `<EmptyState>`：被 /packages、/registrations、/reports、/abnormal-metrics、/group-orders 等多个列表页共用

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useReportStats()`：运营统计（套餐成交量、科室工作量、异常检出率）
- `usePagination()`：通用分页
- `utils/formatReferenceRange.ts`：参考值范围格式化与异常比对
- `utils/calcAge.ts`：根据身份证/生日计算年龄
- `utils/dateFormat.ts`：日期时间格式化

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户角色校验权限
- `error_handler.go`：统一错误响应格式
- `rate_limiter.go`：接口限流
- `request_logger.go`：请求日志（method、path、status、latency）
- `upload.go`：影像检查图片上传处理

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`gbcheckup`
- **前端端口**：18941
- **后端端口**：19941
- **前端框架**：React 18 + TypeScript
- **UI 库**：Ant Design
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL
- **PDF 生成**：`github.com/jung-kurt/gofpdf` 或 `github.com/signintech/gopdf`
- **ORM**：GORM
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/postgres`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # user.ts, package.ts, examinee.ts, registration.ts, examResult.ts, report.ts, abnormalMetric.ts, enterprise.ts
├── stores/           # authStore.ts, userStore.ts, packageStore.ts, registrationStore.ts, reportStore.ts
├── types/            # 共享类型定义
├── components/common/# StatusBadge, AbnormalTag, ReportStatusBadge, ImageUploader, EmptyState, RoleGuard, ErrorBoundary
├── hooks/            # useReportStats.ts, usePagination.ts
├── pages/            # Dashboard, PackageManage, RegistrationManage, ResultEntry, ReportManage, AbnormalMetricTrack, GroupOrderManage, Profile
├── router/           # index.tsx + guards.tsx
├── utils/            # formatReferenceRange.ts, calcAge.ts, dateFormat.ts, request.ts
└── constants/        # report.ts, user.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（数据库连接、JWT、限流、上传、端口等）
    ├── model/                   # 按实体分文件：user.go, package.go, package_item.go, examinee.go, registration.go, exam_result.go, report.go, abnormal_metric.go, enterprise.go, group_order.go
    ├── repository/              # 按实体分文件：user_repository.go, package_repository.go, package_item_repository.go, examinee_repository.go, registration_repository.go, exam_result_repository.go, report_repository.go, abnormal_metric_repository.go, enterprise_repository.go, group_order_repository.go
    ├── service/                 # 按实体分文件：user_service.go, package_service.go, package_item_service.go, examinee_service.go, registration_service.go, exam_result_service.go, report_service.go, abnormal_metric_service.go, enterprise_service.go, group_order_service.go
    ├── handler/                 # 按实体分文件：user_handler.go, package_handler.go, package_item_handler.go, examinee_handler.go, registration_handler.go, exam_result_handler.go, report_handler.go, abnormal_metric_handler.go, enterprise_handler.go, group_order_handler.go
    ├── router/                  # router.go + 按实体分文件：users.go, packages.go, package_items.go, examinees.go, registrations.go, exam_results.go, reports.go, abnormal_metrics.go, enterprises.go, group_orders.go
    ├── middleware/              # auth.go, rbac.go, error_handler.go, rate_limiter.go, request_logger.go, upload.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # report.go, user.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, formatters.go, app_error.go, pdf_generator.go, file_upload.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `EXAM_RESULT_SUBMIT_SUCCESS`、`REPORT_PUBLISH_FAILED`
   - 任何业务字段变更（如 Report 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"Report[no=GB20260816001] publish failed: doctor not allowed"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、异常等级文本、报告状态文本、结果状态文本、角色文本、参考值范围格式化
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **报告状态机必须跨多处定义**
   - ReportStatus 的流转规则在 service 状态机、前端按钮显隐、日志模板、错误码、formatters 中同时存在
   - 新增一个状态值需要修改至少 10 处

5. **枚举必须前后端重复定义且被多处引用**
   - AbnormalLevel 和 ReportStatus 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 PackageItem 新增一个 'critical' 检查项目分组"，必须触达：GORM AutoMigrate、PackageItem 模型、前后端 constants、ExamResultService、AbnormalTag、ReportStatusBadge、formatters、日志模板、错误码、Dashboard 科室工作量统计、套餐配置表单等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbcheckup`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbcheckup`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-gbcheckup}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `18941:80`，后端 `${BACKEND_PORT:-19941}:8080`，数据库 `5432:5432`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbcheckup`
- 数据库连接相关：`DB_NAME=gbcheckup_db`、`DB_USER=gbcheckup_user`、`DB_PASSWORD=gbcheckup_pwd`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=18941`、`BACKEND_PORT=19941`、`DB_PORT=5432`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
frontend/coverage/
uploads/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 AbnormalLevel 和 ReportStatus 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:18941` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<StatusBadge>` 或 `<AbnormalTag>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
